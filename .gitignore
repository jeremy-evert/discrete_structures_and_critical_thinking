.venv/
__pycache__/
*.pyc

# Disposable runtime evidence from the Week 16 validator.
# Preserve durable receipts in sidecar/runs/ and curate any classroom fallback
# fixture into week-16/ rather than committing timestamped validation trees.
artifacts/week16_farkle_validation/
